<view>
	<view class="logo">
		<image src="../../../../images/logo.png" mode=""></image> 
	</view>
	<van-cell-group>
		<van-cell wx:for="{{List}}" wx:key="key" title="{{item.title}}" value="{{item.value}}" />
	</van-cell-group>
</view>